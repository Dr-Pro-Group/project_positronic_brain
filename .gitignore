# Python / virtual environments
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.so
*.egg-info/
.ipynb_checkpoints/

# Tooling / cache
.pytest_cache/
.claude/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Private — kept on disk, never published (manuscript, grant proposal, handover).
# The manuscript goes to arXiv; this repository is its reproduction artifact.
research_paper/
proposal/
HANDOVER.md

# Results ARE published: the JSON records and their written analyses are the
# evidence behind the README's claims, and experiments/ regenerates every figure
# and table from them. Only bulky artefacts (checkpoints, raw console logs) stay local.
runs/*
!runs/*.json
!runs/*.md
trained_models/*.pt
trained_models/*.pt.meta.json
logs/

# The working virtualenv lives here (nested from an older layout).
positronic_brain_v2/

# Python build artifacts
build/
dist/
